# Optional local development switches.
#
# Enable local mock data while running `pnpm dev`.
# WXT_MOCK_DATA=true
#
# Disable the local Edge TTS HTTP helper if needed.
# WXT_EDGE_TTS_HTTP_ENABLED=false
